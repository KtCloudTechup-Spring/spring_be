server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/techup?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: rootroot

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true

  application:
    name: spring-be